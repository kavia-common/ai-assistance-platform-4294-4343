# Backend environment configuration example
BACKEND_PORT=8000
# Use "*" for all origins or provide a comma-separated list
CORS_ORIGINS=*
# Placeholder for future AI provider configuration (e.g., "openai", "anthropic", "local")
MODEL_PROVIDER=local
